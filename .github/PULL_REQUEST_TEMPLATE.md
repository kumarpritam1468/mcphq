## What does this PR do?

<!-- One or two sentences. Link an issue if there is one: "Closes #123". -->

## Why?

<!-- The motivation — not just what changed, but why it's needed. -->

## Checklist

- [ ] `bun test` passes
- [ ] `bun run build` passes (typecheck across all packages)
- [ ] `bun run lint` passes, or pre-existing failures I didn't introduce are noted below
- [ ] If this adds/changes an adapter: `packages/core/src/adapters/CONFIG_LOCATIONS.md` is updated to match
- [ ] If this adds a new mutating command/flag: it supports `--dry-run`

## How was this tested?

<!-- New/changed automated tests, and any manual verification (e.g. against a real installed client). -->
